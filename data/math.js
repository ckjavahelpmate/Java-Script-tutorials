
const user = {
    id: 101,
    name: 'John Doe',
    email: 'john.doe@example.com',
    address: {
        street: '123 Main St', 
        city: 'Anytown',
        country: 'USA'
    },
    hobbies: ['reading', 'traveling', 'gaming']
};


export function power(base, exponent) {
    return Math.pow(base, exponent);
}

export function add(a, b) {
    return a + b;
}
export default function multipy(a, b) {
    return a * b;
}