/*
console.log("Welcome to java Script");

1. Chapter-01 :: Data types
---------------------------
a) Primitive Data types
    1. Number
    2. Boolean
    3. String
    4. indefined
    5. null
    
b) Non-Primitive data types
    1. Object
    2. Array
    3. class

2. Chapter-02 :: Variable
---------------------------
1. var -> can be re-declared and re-initialized
------------------------------------------------
        var age = 10 ;
        console.log( age ); // 10

        var age = 20 ; // re-declared  
        console.log( age ); // 20

        age = 30 ; // re-initialized
        console.log( age ); // 30

2. let -> can not be re-declared but can be re-initialized
----------------------------------------------------------
        let age = 10 ;
        console.log( age ); // 10

        age = 20 ; // re-initialized
        console.log( age ); // 20

        let age = 30 ; // syntax error -> can not be re-declared

3. const -> can not be re-declared or re-initialized
----------------------------------------------------
        const age = 25 ;
        console.log( age ); // 25

        age = 30 ; //syntax error -> can not be re-initialized

        const age = 30 ; // syntax error -> can not be re-declared
*/